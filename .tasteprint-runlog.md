# Tasteprint Run Log

## 2026-03-23 (pass 30)
- **What:** Removed dead `texture` prop and `getTextureStyle` import from `CodeIDE.jsx` (7000+ line file). Removed: (1) `import { getTextureStyle } from "../../utils"`, (2) `texture` from the destructured props signature, (3) `...getTextureStyle(texture, p)` spread in the root div's style object. This was the last file in the codebase still using the dead texture prop.
- **Why:** Priority 1 (lint fix / dead code). Pass 29 explicitly noted CodeIDE was left for a future pass due to file size. `texture` was never passed from `ComponentRenderer` (removed from `CRaw`'s params in pass 17), so it was always `undefined`. `getTextureStyle(undefined, p)` returned `{}`, making the spread a no-op every render. The unused `texture` param would fail `no-unused-vars` with `--max-warnings=0`.
- **Risk:** None. Completes the texture removal started in pass 13. Behavior-identical — removes one dead import, one unused param, and one empty-object spread per render.
- **Files changed:** `src/components/code/CodeIDE.jsx`

## 2026-03-23 (pass 29)
- **What:** Completed dead `texture` prop removal across ComponentRenderer and 8 code variant components. (1) Removed `texture={texture}` from all code component JSX calls in `ComponentRenderer.jsx` (CodeIDE, CodeTypewriter, CodeNotebook, CodeReview, CodeMinimap, AsciiArt, CodeBenchmark) and ChatBubble — these were ReferenceErrors since `texture` was removed from CRaw's params in pass 17 but the JSX still referenced it. (2) Removed `getTextureStyle` import, `texture` prop, and `getTextureStyle(texture, ...)` usage from tokenizer.jsx, CodeNotebook.jsx, CodeReview.jsx, AsciiArt.jsx, CodeBenchmark.jsx, CodeTypewriter.jsx, and CodeMinimap.jsx. (3) CodeIDE.jsx (7000+ lines) still retains `texture` prop and `getTextureStyle` import — left for a future pass due to file size.
- **Why:** Priority 1 (bug fix). The `texture={texture}` references in ComponentRenderer were ReferenceErrors — `texture` was removed from `CRaw`'s parameter list in pass 17, so the variable didn't exist in scope. Any code-block component (variants 3-10) or chat component would crash on render. The code variant files themselves had dead `getTextureStyle(undefined, p)` calls producing empty object spreads every render.
- **Risk:** None for the bug fix. The texture feature was fully removed starting at pass 13. All `getTextureStyle(texture, ...)` calls produced `{}` since `texture` was always `undefined`. Removing them is behavior-identical.
- **Files changed:** `src/components/ComponentRenderer.jsx`, `src/components/code/tokenizer.jsx`, `src/components/code/CodeNotebook.jsx`, `src/components/code/CodeReview.jsx`, `src/components/code/AsciiArt.jsx`, `src/components/code/CodeBenchmark.jsx`, `src/components/code/CodeTypewriter.jsx`, `src/components/code/CodeMinimap.jsx`

## 2026-03-23 (pass 28)
- **What:** Added `libEntry` test to `usePicky.test.js` — covers lookup of known types (returns correct LIB item with type/w/h/label) and unknown types (returns fallback with type-as-label, 300×200 defaults). This validates the shared utility after pass 27's deduplication.
- **Why:** Priority 1 (test coverage). `libEntry` is now a shared export used by both `usePicky` (internally for assembly/option generation) and `PickyOverlay` (for preview sizing). If the fallback or lookup logic breaks, both Picky's assembly and its UI previews break silently. The test catches regressions.
- **Risk:** None. New test assertions only, no code changes.
- **Files changed:** `src/__tests__/usePicky.test.js`

## 2026-03-23 (pass 27)
- **What:** Deduplicated `libEntry` function. Exported it from `usePicky.js` (the canonical location, already used internally by `assembleShapes` and `generateOptions`), and replaced the identical copy in `PickyOverlay.jsx` with an import. Also removed the now-unused `LIB` import from `PickyOverlay.jsx` (it was only used by the local `libEntry`). Added `label` field to the usePicky fallback return to match PickyOverlay's version.
- **Why:** Priority 4 (simplification). Two identical 6-line functions doing the same LIB lookup is a maintenance hazard — if LIB structure changes or fallback defaults need updating, both copies must stay in sync. Single source of truth is better. Also removes an unused import (`LIB` from constants), which would fail `--max-warnings=0` lint.
- **Risk:** None. Both functions had identical logic. The exported version now includes the `label` field in its fallback (matching PickyOverlay's version), which is a superset of what internal callers needed.
- **Files changed:** `src/hooks/usePicky.js`, `src/components/PickyOverlay.jsx`

## 2026-03-23 (pass 26)
- **What:** Removed dead `texture` prop and `getTextureStyle` import from `ChatBubble.jsx`. The `texture` parameter was in the component signature and spread via `...getTextureStyle(texture, p)` in all 7 variants, but was never passed from `ComponentRenderer` (which doesn't include `texture` in `CRaw`'s destructured props). Every call was `getTextureStyle(undefined, p)` → `{}`, producing an empty spread on every render.
- **Why:** Priority 4 (simplification). Completes the texture prop removal across all components (passes 13-14 cleaned ShapeItem/LibrarySidebar/PropsPanel, pass 17 cleaned the CRaw signature). ChatBubble was the last file still importing and using the dead prop. Removing it makes the component API honest and eliminates 7 unnecessary function calls per render.
- **Risk:** None. `texture` was always `undefined`. `getTextureStyle` remains exported from `utils.js` and its test still passes.
- **Files changed:** `src/components/ChatBubble.jsx`

## 2026-03-23 (pass 25)
- **What:** Added `src/__tests__/ErrorBoundary.test.jsx` — tests for the `ErrorBoundary` component. Covers: rendering children normally, showing default error UI with Retry button when a child throws, rendering custom fallback prop, recovery after Retry click + re-render with non-throwing child, and verifying error logging to console.
- **Why:** Priority 1 (test coverage). `ErrorBoundary` wraps every `ComponentRenderer` call on the canvas and in the library sidebar — it's the crash-safety net for all 40+ component types. The Retry button is user-facing UI that had no test coverage. If ErrorBoundary's state reset logic broke, users would see permanent "Component error" with no recovery path.
- **Risk:** None. New test file only, no code changes.
- **Files added:** `src/__tests__/ErrorBoundary.test.jsx`

## 2026-03-23 (pass 24)
- **What:** Added `src/__tests__/usePicky.test.js` — unit tests for the pure functions and data exports from `usePicky.js`. Tests cover: `paletteTemperature` (warm/cool/neutral classification), `PAGE_TEMPLATES` integrity (unique IDs, required fields, minimum slots), `PICKY_MOODS` and `QUICK_PRESETS` validation, `CUSTOM_SLOT_OPTIONS` dedup, `assembleShapes` (empty picks, positioned stacking, phone vs desktop width, variant preservation, slot skipping, entrance animation delays), and `loadPickyHistory` (empty, stored, corrupt, and max-3 limit).
- **Why:** Priority 1 (test coverage). `usePicky` is the most complex state machine in the codebase (280+ lines, 9 page templates, 6 moods, custom builder, assembly layout logic) and had zero dedicated tests. The `assembleShapes` function handles 4 layout strategies (full-width, sidebar+content, small-grid, default centered) and is the most likely to break when templates or component types change. These tests catch regressions in template data integrity and layout positioning without needing to render the full PickyOverlay UI.
- **Risk:** None. New test file only, no code changes. Note: shell exec unavailable so verify not run — tests use standard import + assertion patterns matching existing test suite.
- **Files added:** `src/__tests__/usePicky.test.js`

## 2026-03-23 (pass 23)
- **What:** Added `src/__tests__/useKeyboard.test.js` — comprehensive tests for the `useKeyboard` hook. Covers all keyboard shortcuts: Delete/Backspace (onDel), Cmd+Z/Cmd+Shift+Z (undo/redo), Cmd+D (duplicate), Cmd+G/Cmd+Shift+G (group/ungroup), single-key designer shortcuts (R, Shift+R, U, M, L, Z, C), ArrowLeft/Right for cycleVariation, Arrow nudge (1px and 10px with Shift), and the Shift+Arrow fix from pass 22. Also tests that designer shortcuts are suppressed during text editing (contenteditable focus).
- **Why:** Priority 1 (test coverage). `useKeyboard` handles every keyboard shortcut in the app and had zero test coverage. The bug fixed in pass 22 (Shift+Arrow intercepted by cycleVariation) would have been caught by these tests. The new test explicitly asserts that Shift+Arrow nudges instead of cycling variants.
- **Risk:** None. New test file only, no code changes. Note: shell exec unavailable so verify not run — tests use standard renderHook + window.dispatchEvent patterns matching existing test suite.
- **Files added:** `src/__tests__/useKeyboard.test.js`

## 2026-03-23 (pass 22)
- **What:** Fixed `Shift+Arrow` nudge not working on shapes with variant candidates. In `useKeyboard.js`, the `ArrowLeft`/`ArrowRight` → `cycleVariation` handler didn't check `!e.shiftKey`, so `Shift+ArrowLeft/Right` (10px nudge) was intercepted and cycled the variant instead. Added `!e.shiftKey` guard so Shift+Arrow always nudges.
- **Why:** Priority 1 (bug fix). Shift+Arrow is a core designer shortcut for coarse nudging. On any shape with multiple randomize candidates (i.e. after randomizing), the shortcut silently did the wrong thing — cycling variants instead of moving the shape. This is a silent UX regression that's hard to notice but frustrating in practice.
- **Risk:** None. The cycleVariation shortcut (unshifted Arrow) still works. Shift+Arrow now correctly falls through to the nudge handler below.
- **Files changed:** `src/hooks/useKeyboard.js`

## 2026-03-22 (pass 21)
- **What:** Added `src/__tests__/ComponentRenderer.test.jsx` — comprehensive smoke tests for the ~2200-line ComponentRenderer module. Tests every component type × every variant combination (300+ individual render assertions), plus edge cases: unknown type, out-of-range variant, editable mode, dark palette (luminance calc paths), and non-default font/fsize.
- **Why:** Priority 1 (test coverage). ComponentRenderer was the single largest source file with zero test coverage. It renders all 40+ component types and is the most crash-prone surface — any variant addition or prop change can silently break rendering. These smoke tests catch crash regressions across the entire component catalog. Note: shell exec unavailable so verify not run — tests use standard render patterns matching existing test suite.

## 2026-03-22 (pass 20)
- **What:** Stabilized `enterPicky`, `exitPicky`, and `cancelPicky` callbacks by reading from `pickyRef.current` instead of closing over the `picky` object. Removed `picky` from all three dependency arrays (now `[]`, `[push]`, and `[]` respectively).
- **Why:** Priority 5 (performance). `usePicky` returns a new object every render, so all three callbacks were recreated on every state change. `enterPicky` and `cancelPicky` are passed to `Header` (which is `React.memo`'d since pass 7), defeating memo on every render. With `pickyRef`, these callbacks are stable and Header skips unnecessary re-renders.
- **Risk:** None. Same ref-at-call-time pattern used in passes 8-12. The picky methods are event handlers (not render-path), so reading refs at call time is safe.
- **Files changed:** `src/App.jsx`

## 2026-03-22 (pass 19)
- **What:** Removed unused `lockedShapes: _lockedShapes` destructuring from App.jsx's `useDesigner` call. The value was extracted but never read — `useDesigner` handles locked-shape filtering internally (in `randomizeAll`), so App never needs it.
- **Why:** Priority 4 (simplification). Dead bindings add noise when reading App.jsx. The underscore prefix was only there to suppress the lint error.
- **Risk:** None. `lockedShapes` is still returned from `useDesigner` (tests assert it), just no longer destructured in App.
- **Files changed:** `src/App.jsx`

## 2026-03-22 (pass 18)
- **What:** Removed dead `_hasRndUndo` state from `useDesigner`. The state was set via `setHasRndUndo(true/false)` in `randomize`, `randomizeAll`, `undoRandomize`, and `resetDesignerState`, but was never read or returned from the hook. The actual undo capability is driven by the `rndUndo` ref, which `undoRandomize` reads directly.
- **Why:** Priority 5 (performance). Each `setHasRndUndo` call triggered a state update → re-render of the entire App tree. Since the value was never consumed, these were pure waste. Every single-shape randomize caused 1 unnecessary re-render, every randomize-all caused 1, and every undo caused 1. With this removed, those operations no longer trigger a bonus re-render cycle.
- **Risk:** None. The state was write-only — never read, never returned, never consumed by any component or test. The underscore prefix was masking it from ESLint's `no-unused-vars`.
- **Files changed:** `src/hooks/useDesigner.js`

## 2026-03-21 (pass 17)
- **What:** (1) Removed dead `texture` parameter from `CRaw` function signature in `ComponentRenderer.jsx`. The param was left behind when passes 13-14 removed texture usage from `ShapeItem` and `PropsPanel` — callers no longer pass it, so it's an unused destructured arg that would fail `no-unused-vars` with `--max-warnings=0`. (2) Removed duplicate `@keyframes tp-tooltip-in` from `global.css` — the same keyframe is already defined in `components/animations.css` (which is imported by `ComponentRenderer`). Having two definitions with different `translateY` directions (`-4px` in global vs `4px` in animations) was fragile; the animations.css version is the authoritative one for component use.
- **Why:** Priority 1 (lint fix / warning reduction). The unused `texture` param would block `npm run verify` since ESLint reports unused trailing function parameters. The duplicate keyframe is a CSS hygiene issue that could cause inconsistent animation direction depending on stylesheet load order.
- **Risk:** None. `texture` was already dead (never passed by any caller). The `tp-tooltip-in` keyframe in `animations.css` remains available globally since `ComponentRenderer` is always rendered.
- **Files changed:** `src/components/ComponentRenderer.jsx`, `src/global.css`

## 2026-03-21 (pass 16)
- **What:** Fixed duplicate control rendering in `PropsPanel`. Many generic toggle blocks (no type guard) shared React keys with type-specific blocks below them, causing components like `media-player`, `chart`, `profile-card`, `skeleton`, etc. to render TWO identical toggles for the same prop. Also removed a literally copy-pasted duplicate `compact` block. Added a key-based dedup pass at the end of the controls array — last entry wins, so type-specific blocks (with custom labels/colors) correctly override generic fallbacks.
- **Why:** Priority 2 (bug fix / UI quality). Duplicate controls cluttered the props panel for ~15+ component types and could confuse designers. The duplicate `compact` block was dead code.
- **Risk:** Low. The dedup preserves the type-specific block's rendering (last wins). No behavior change for components with only one block per key. The array iteration is trivial overhead.
- **Files changed:** `src/components/PropsPanel.jsx`

## 2026-03-21 (pass 15)
- **What:** Removed stale `texture` reference from `<C>` render call in `ShapeItem.jsx`. The `texture` variable was removed from the component's destructured props in pass 13, but the JSX still passed `texture={texture}` to `<C>`, which would cause a `ReferenceError` at runtime whenever a shape rendered.
- **Why:** Priority 1 (bug fix). This is a regression from pass 13's dead-prop cleanup — the import and prop were removed from the signature but one usage in the JSX was missed.
- **Risk:** None. `texture` was already dead/undefined before pass 13; removing the reference just completes the cleanup.
- **Files changed:** `src/components/ShapeItem.jsx`

## 2026-03-21 (pass 14)
- **What:** Removed dead `texture` prop and `getTextureStyle` import from `PropsPanel`. Same cleanup as pass 13 — `texture` was never passed from the parent (`ShapeItem` renders `<PropsPanel>` without it).
- **Why:** Priority 4 (simplification). Completes the texture prop removal across all components. PropsPanel was calling `getTextureStyle(undefined, p)` on every render, producing an empty object spread into the style.
- **Risk:** None. `getTextureStyle` remains in `utils.js` and its test still passes.
- **Files changed:** `src/components/PropsPanel.jsx`

## 2026-03-21 (pass 13)
- **What:** Removed dead `texture` prop from `ShapeItem` and `LibrarySidebar`. Removed from: component signatures, memo comparators, inline style spreads, and `getTextureStyle` imports. The prop was never passed from `App.jsx` (always `undefined`).
- **Why:** Priority 4 (simplification of visible complexity). Dead props confuse contributors reading the code, add unnecessary entries to memo comparators, and the `getTextureStyle(undefined, p)` calls produced empty objects on every render. Removing them makes the component API honest about what's actually used.
- **Risk:** None. `texture` was never passed; behavior is identical. `getTextureStyle` remains exported from `utils.js` for future use.
- **Files changed:** `src/components/ShapeItem.jsx`, `src/components/LibrarySidebar.jsx`

## 2026-03-21
- **What:** Updated `.gitignore` to exclude OpenClaw workspace files (AGENTS.md, SOUL.md, USER.md, IDENTITY.md, TOOLS.md, MEMORY.md, HEARTBEAT.md, .clawhub/, .openclaw/, YourBud/, memory/) and large media assets (*.mp4, *.mov, etc.) from version control. Also added `.tasteprint-runlog.md` to ignored logs.
- **Why:** Repo has zero commits. Before initial commit, .gitignore must prevent workspace/agent files and demo videos from polluting the source tree. This is the highest-priority repo stabilization step — ensures clean git history from day one.
- **Risk:** None. Only affects what git tracks, no code changes.

- **What:** Fixed `public/manifest.json` to use relative paths (`"./"`) instead of absolute (`"/"`) for `start_url`, `scope`, and `id`.
- **Why:** Absolute paths break PWA install/scope when deployed under a subpath (e.g. GitHub Pages at `/Tasteprint/`). Relative paths work correctly for both root and subfolder deploys, matching the existing `VITE_BASE_PATH` strategy documented in README and `.env.example`.
- **Risk:** None. Relative manifest paths are spec-compliant and resolve against the manifest URL. No code changes, only the static manifest.

## 2026-03-21 (pass 2)
- **What:** Added `CONTRIBUTING.md` with quick start, design principles, PR guidance, project structure overview, and component-addition recipe.
- **Why:** Priority 3 (designer-first onboarding). The repo had no contributor guide. This file codifies the "simple surface, powerful core" philosophy so future contributors don't accidentally flatten progressive disclosure or add config-heavy features. Also serves as a lightweight architecture map for new devs.
- **Risk:** None. New file only, no code changes.

## 2026-03-21 (pass 3)
- **What:** Added `.nvmrc` (contents: `22`) so `nvm use` in the Quick Start actually works.
- **Why:** Priority 1 (repo stabilization). README tells contributors to run `nvm use` but the repo had no `.nvmrc`, causing an error. The `engines` field already pins Node >=22 <23, so `.nvmrc` just aligns the two.
- **Risk:** None. New file, no code changes.

## 2026-03-21 (pass 5)
- **What:** Added "Design Moods" section to README.md documenting all 30 design moods with categorized table. Also fixed stale root `manifest.json` to use relative paths (matching `public/manifest.json`).
- **Why:** Priority 3 (README/product positioning). The 30 design moods are a major differentiating feature but were invisible in the README — users had to discover them in the UI. The mood table makes the feature discoverable and shows depth. The root manifest fix is a minor hygiene item (file is already gitignored but had outdated absolute paths).
- **Risk:** None. README text addition only + manifest path alignment.

## 2026-03-21 (pass 12)
- **What:** Stabilized `toCanvas` by replacing `device` closure read with `deviceRef.current`. Removed duplicate `deviceRef` declaration in the tp API section (now shares the one declared earlier). This cascades: `onDown` and `onDrop` both depend on `toCanvas`, so they're now fully stable too.
- **Why:** Priority 5 (performance). `toCanvas` was recreated every time `device` changed (rare), but more importantly its instability prevented `onDown` from being stable. `onDown` is passed to every `ShapeItem` — now that it's stable, `ShapeItem`'s memo can skip re-renders when only shapes/selection change but the shape itself didn't.
- **Risk:** None. `deviceRef` was already used in the tp API section. Consolidated to a single declaration near the top of the function.
- **Files changed:** `src/App.jsx`

## 2026-03-21 (pass 11)
- **What:** Stabilized `cycleMood` (removed `designMood` dep, now reads `designMoodRef`). Extracted the inline `<style>` string to a module-level `GLOBAL_STYLES` constant so React doesn't diff a new string on every render.
- **Why:** Priority 5 (performance/hygiene). `cycleMood` is passed to `useKeyboard` — while useKeyboard already refs its props internally, keeping the callback stable reduces allocation churn. The global styles string is ~800 chars of static CSS recreated as a new string literal every render; hoisting it means the JSX `<style>` element gets the same reference and React skips the DOM update.
- **Risk:** None. `cycleMood` was the last closure-captured state dep callback. The styles string is identical — just moved outside the function.
- **Files changed:** `src/App.jsx`

## 2026-03-21 (pass 10)
- **What:** Stabilized `randomize` and `randomizeAll` — the two largest remaining unstable callbacks. Both previously had 5-item dependency arrays (`[shapes, p, designMood, prefV, selAll/lockedShapes]`) and are now `[]`. Added `designMoodRef`, `pRef`, and `lockedShapesRef` refs to support this. All state reads are now via refs at call time.
- **Why:** Priority 5 (performance). `randomize` is passed to every `ShapeItem` and also used by `useKeyboard`. `randomizeAll` is passed to `Header`. Both were recreated on every state change (shapes, palette, mood, prefV, selection, locked set), defeating child component memoization across the entire tree. This is the single biggest callback stabilization win — these two had the most deps and the widest prop-passing footprint.
- **Risk:** Low. Same ref-at-call-time pattern used in passes 8-9 and runs 29-31. All refs are defined and synced. `randomize`/`randomizeAll` are event handlers (not render-path), so reading refs at call time is safe.
- **Files changed:** `src/App.jsx`

## 2026-03-21 (pass 9)
- **What:** Stabilized four more callbacks: `delShape`, `onDel`, `dupShape`, and `onMove` — all previously had `selAll` in their dependency arrays. Now they read from `selAllRef.current` at call time.
- **Why:** Priority 5 (performance). `selAll` (a Set) changes on every click/shift-click selection change. These callbacks were recreated each time, defeating memo on child components. `onMove` is especially hot — called on every mousemove during drag — and its `selAll` dep was unnecessary since selection doesn't change mid-drag. With `selAllRef`, `onMove`'s deps drop from 6 to 5 (removing the one that doesn't change during drag but still triggers recreation on click).
- **Risk:** Low. Same ref pattern. `selAllRef` was already defined and synced (from pass 31). Selection state is always current at call time via the ref.
- **Files changed:** `src/App.jsx`

## 2026-03-21 (pass 8)
- **What:** Stabilized three more callbacks in `App.jsx`: `undoDesign`, `copyStyle`, and `cycleVariation`. All three previously closed over the `shapes` array (and `cycleVariation` also over `candidates`, `candidateIdx`, `prefV`), causing them to be recreated on every state change. Now they read from refs (`shapesRef`, `candidatesRef`, `candidateIdxRef`) at call time and have empty or minimal dependency arrays.
- **Why:** Priority 5 (performance). These callbacks are passed as props to `ShapeItem` (which is `memo`'d). When they recreate, every shape on the canvas gets a new prop and re-renders. With stable callbacks, `ShapeItem`'s memo can skip re-renders for shapes that haven't changed. Continues the callback stabilization pattern from runs 29-31.
- **Risk:** Low. Same ref-at-call-time pattern used throughout the codebase. `undoDesign` reads `shapesRef.current` inside a `setPrefV` updater. `copyStyle` reads it at the top of the function. `cycleVariation` uses new `candidatesRef`/`candidateIdxRef` refs.
- **Files changed:** `src/App.jsx`

## 2026-03-21 (pass 7)
- **What:** Wrapped `Header` with `React.memo` and replaced the `shapes` array prop with `shapeCount` (a number). Removed the unused `shapes` parameter from `reflowForDevice` (it already uses `setShapes(prev => ...)` functional updates).
- **Why:** Priority 5 (performance). Header received the full `shapes` array, causing it to re-render on every drag, resize, and nudge — even though it only used `shapes.length` for button enable/disable and `reflowForDevice` never read the array. Now Header only re-renders when the count changes (add/remove) or other scalar props change. Combined with the already-stabilized callbacks from pass 31, this eliminates the most frequent unnecessary re-renders of the toolbar.
- **Risk:** Low. `reflowForDevice` already used functional `setShapes(prev => ...)` so it never needed the `shapes` param. All `shapes.length` checks are replaced with the equivalent `shapeCount` number. `React.memo` is a safe shallow-comparison wrapper.
- **Files changed:** `src/components/Header.jsx`, `src/App.jsx`


- **What:** Added missing `@keyframes tp-tooltip-in` to the global `<style>` block in `App.jsx`.
- **Why:** Priority 1 (bug fix / warning reduction). `Header.jsx` references `animation: "tp-tooltip-in .15s ease-out both"` on the mood picker dropdown and mobile menu overlay, but the keyframe was never defined. Menus appeared instantly instead of animating in. Added a subtle fade+slide-up entrance animation consistent with the existing `tp-rnd-toast` pattern.
- **Risk:** None. Purely additive CSS keyframe — no logic changes. Menus that previously popped in now animate smoothly.

## 2026-03-21 (pass 4)
- **What:** Updated `vitest.config.js` to merge with `vite.config.js` via `mergeConfig`, so tests share the same `@vitejs/plugin-react` plugin instead of relying on esbuild's implicit JSX handling.
- **Why:** Priority 4 (build hygiene). The standalone vitest config worked because esbuild can handle JSX, but it bypassed the React plugin's automatic JSX runtime and any Vite-specific transforms. Merging ensures tests run in the same environment as the dev/build pipeline — prevents subtle mismatches and makes the config more maintainable (single source of truth for plugins).
- **Risk:** Low. `mergeConfig` is Vite's official API for this pattern. The `vite.config.js` export is a function `({ mode }) => config`, so we call it with `{ mode: "test" }`. If tests break, reverting is a single-file change.
